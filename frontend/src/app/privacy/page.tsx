import type { Metadata } from "next";
import PolicyPage from "../components/PolicyPage";
export const metadata: Metadata = { title: "Privacy", description: "Privacy information for The Republic Bulletin." };
export default function PrivacyPage() { return <PolicyPage eyebrow="Reader information" title="Privacy" description="How this website handles information provided by readers and newsroom users." sections={[{ title: "Information you provide", content: <p>Account registration may store a username, optional email address, password hash, profile information, comments, and notification state. Newsletter signup stores the submitted email address and signup source.</p> }, { title: "Operational data", content: <p>The service may process standard server logs and aggregate article view counts for security, reliability, and editorial analytics. It should not be configured to collect unnecessary personal information.</p> }, { title: "Cookies", content: <p>Authentication uses an HTTP-only session cookie. Reader display preferences may be stored in the browser. Third-party advertising or analytics cookies apply only when the publisher explicitly configures those services.</p> }, { title: "Your choices", content: <p>Readers can update their profile and password from their account. Requests about stored information should be sent through the configured general contact channel.</p> }, { title: "Configuration notice", content: <p className="configuration-note">The publisher should review this page with qualified legal counsel and add jurisdiction-specific retention, data-controller, and rights information before relying on it as a final legal policy.</p> }]} />; }
