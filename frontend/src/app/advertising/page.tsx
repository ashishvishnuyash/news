import type { Metadata } from "next";
import PolicyPage from "../components/PolicyPage";
import { getSiteSettings } from "../../lib/news";
export const metadata: Metadata = { title: "Advertising", description: "Advertising standards and enquiries for The Republic Bulletin." };
export default async function AdvertisingPage() { const settings = await getSiteSettings(); const address = settings.contact?.advertising; return <PolicyPage eyebrow="Commercial information" title="Advertising" description="Advertising architecture that preserves a clear boundary between commercial and editorial work." sections={[{ title: "Labelling and independence", content: <p>Paid placements must be labelled as advertising or sponsored content. Commercial partners do not receive control over newsroom reporting, conclusions, or corrections.</p> }, { title: "Available placements", content: <p>The platform supports configurable header, homepage, in-feed, article, sidebar, and footer slots. Slots remain absent when advertising is disabled or no approved creative is configured.</p> }, { title: "Enquiries", content: address ? <p>Email <a href={`mailto:${address}`}>{address}</a> for current availability and technical requirements.</p> : <p className="configuration-note">An advertising contact address and rate information have not yet been configured.</p> }]} />; }
